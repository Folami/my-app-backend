{
  "name": "my-app-backend",
  "version": "1.0.0",
  "description": "'Notes App Backend'",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "\"echo \\\"Error: no test specified\\\" && exit 1\"",
    "build:ui": "rm -rf build && cd ../my-app/ && npm run build && cp -r build ../my-app-backend",
    "deploy": "git push heroku main",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",    
    "logs:prod": "heroku logs --tail"
  },
  "proxy": "http://localhost:3001",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Folami/sample-server.git"
  },
  "author": "BIDMUS Folami",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Folami/sample-server/issues"
  },
  "homepage": "https://github.com/Folami/sample-server#readme",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
